<Page x:Class="MCM6000_UI.AllowedDevices"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MCM6000_UI"
        mc:Ignorable="d"
        Title="" Height="400" Width="700">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/ThemeControl;component/ThorStyleDark.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!--Used for populating combobox in system tabs for device id's-->
            <XmlDataProvider x:Key="Device_id_XmlData"/>
        </ResourceDictionary>
    </Page.Resources>
    <Grid Background="#FF333333">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid Margin="5,5,5,5" x:Name="e_ad_allowed" ItemsSource="{Binding Entries}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Slot Type" Binding="{Binding SlotType}" Width="200" IsReadOnly="True" />
                <DataGridTextColumn Header="Device ID" Binding="{Binding DeviceID}" Width="150" IsReadOnly="True" />
                <DataGridCheckBoxColumn Header="Remove" Binding="{Binding ToRemove}" />
            </DataGrid.Columns>
        </DataGrid>
        <Border Margin="5,5,5,5" BorderBrush="White" BorderThickness="2" Grid.Row="1" >
            <StackPanel Orientation="Vertical">
                <Grid x:Name="grid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="5,5,5,5">
                        <Label Content="[ Add Device Signature ]"/>
                        <ComboBox x:Name="e_ad_slot_id" Margin="10,0,0,0" Width="150" />
                        <TextBox x:Name="e_ad_device_id" Margin="20,0,0,0" Width="{Binding ActualWidth, ElementName=e_ad_slot_id, Mode=OneWay}"/>
                        <Button x:Name="e_ad_add" Margin="10,0,0,0" Content="Add" Width="30" Click="AddEntry"/>
                    </StackPanel>
                    <Button x:Name="e_ad_remove" Content="Remove Selected" Margin="5,5,5,5" Grid.Column="2" Click="RemoveEntries"/>
                </Grid>
                <Grid Height="{Binding ActualHeight, ElementName=grid}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <Button x:Name="e_ad_load_file" Content="Load From File" Margin="5,5,5,5" Click="LoadFromFile" />
                    <Button x:Name="e_ad_commit" Content="Save" Margin="5,5,5,5" Grid.Column="2" Click="Commit" />
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
